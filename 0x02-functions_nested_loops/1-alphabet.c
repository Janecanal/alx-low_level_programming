#include "main.h"

/**
 * main - entry point
 *
 * Description - A function that prints the alphabet in lowercase
 *
 * Return: Always 0
 *
 */

int main(void)
{
char lowercase;
for (lowercase = 'a'; lowercase <= 'z';)
lowercase++;
{
	_putchar(lower);
}
_putchar('\n');

return (0);
}
